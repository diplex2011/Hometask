class Boxer:

    def __init__(self, age, weight, height):
        self.age = age
        self.weight = weight
        self.height = height

    def kick(self):
        return "I will kill you!!!"

    def pull_up(self):
        return "I can make 55 pull-ups!!1"

vasily = Boxer(25, 100, 150)


class Person:

    def __init__(self, age, first_name, second_name, birthday_month):
        self.age = age
        self.first_name = first_name
        self.second_name = second_name
        self.birthday_month = birthday_month

    def full_name(self):
        return self.first_name + ' ' + self.second_name

    def email(self):
        return self.first_name + '.' + self.second_name + '@email.com'

    def birthday_season(self):
        if self.birthday_month in ['January', 'February', 'December']:
            return 'Winter'
        elif self.birthday_month in ['March', 'April', 'May']:
            return 'Spring'
        elif self.birthday_month in ['June', 'July', 'August']:
            return 'Summer'
        elif self.birthday_month in ['September', 'October', 'November']:
            return 'Fall'

body = Person(18, 'Pedro', 'Villalobos', 'March')
